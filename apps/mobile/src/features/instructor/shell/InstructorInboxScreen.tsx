import { PlaceholderScreen } from "./PlaceholderScreen";

/** Step-04 seam: the typed-card inbox and broadcast history (architecture §4a.4) land here. */
export function InstructorInboxScreen() {
  return (
    <PlaceholderScreen
      title="Inbox"
      note="Student conversations and your broadcasts arrive with the next step."
    />
  );
}
