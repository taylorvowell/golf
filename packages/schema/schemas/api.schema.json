{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://swingsage.dev/schemas/api.schema.json",
  "title": "Api",
  "description": "Every JSON request and response body of the versioned HTTP API, in one place.\n\nThis file exists because of one fact: a native app cannot be force-updated. Once a build is in a store, old versions keep calling these routes for months, so an unannounced change to a body is an outage for someone who cannot install the fix. Bodies are therefore evolved ADDITIVELY inside a version, and a shape that genuinely cannot be evolved gets a new version prefix instead.\n\n`ApiVersion`, `ClientConfig` and `UpgradeRequired` below are the negotiation itself — what a client sends, what the server answers, and how the server says 'you are too old to be safe'.",
  "type": "object",
  "properties": {
    "_unused": {
      "type": "null",
      "description": "This document is a definitions container; the API has no single root body. Types come from `definitions`."
    }
  },
  "definitions": {
    "apiVersion": {
      "title": "ApiVersion",
      "type": "string",
      "enum": [
        "v1"
      ],
      "description": "The path segment every route lives under (`/api/v1/...`). Adding a member here is how a breaking change ships: the old version keeps answering for its whole support window while the new one is adopted."
    },
    "apiError": {
      "title": "ApiError",
      "type": "object",
      "description": "The one error envelope. A machine-readable `error` code plus a human `message` — clients switch on the code, and never on the prose, which is free to change.",
      "required": [
        "error"
      ],
      "properties": {
        "error": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      }
    },
    "upgradeRequired": {
      "title": "UpgradeRequired",
      "type": "object",
      "description": "The body of a 426. The escape hatch for the case where compatibility is genuinely impossible — it should be rare, and it must exist, because the alternative is a client that fails every request with no way to explain why.\n\nA client that receives this must show a real screen, not a failed request.",
      "required": [
        "error",
        "message",
        "minimumVersion",
        "currentVersion",
        "storeUrl"
      ],
      "properties": {
        "error": {
          "type": "string",
          "enum": [
            "upgrade_required"
          ]
        },
        "message": {
          "type": "string"
        },
        "minimumVersion": {
          "type": "string",
          "description": "The oldest client build still served, as `major.minor.patch`."
        },
        "currentVersion": {
          "type": "string",
          "description": "The newest build the server knows about — what the store is expected to offer."
        },
        "storeUrl": {
          "type": [
            "string",
            "null"
          ],
          "description": "Where to go and update. Null when the platform is unknown, in which case the client falls back to its own store link."
        }
      }
    },
    "clientConfig": {
      "title": "ClientConfig",
      "type": "object",
      "description": "What a client asks for at launch, before it trusts anything else. Unauthenticated on purpose: a client too old to authenticate must still be able to learn that it is too old.",
      "required": [
        "apiVersion",
        "minimumVersion",
        "currentVersion",
        "supportedApiVersions",
        "deprecatedApiVersions",
        "minimumArtifactSchema",
        "currentArtifactSchema"
      ],
      "properties": {
        "apiVersion": {
          "$ref": "#/definitions/apiVersion"
        },
        "minimumVersion": {
          "type": "string"
        },
        "currentVersion": {
          "type": "string"
        },
        "supportedApiVersions": {
          "type": "array",
          "description": "Every version prefix still answering, newest last. A version leaves this list only after its published deprecation window closes.",
          "items": {
            "$ref": "#/definitions/apiVersion"
          }
        },
        "deprecatedApiVersions": {
          "type": "array",
          "description": "Still answering, but scheduled for removal. A client on one of these is told by the `Deprecation` and `Sunset` response headers as well.",
          "items": {
            "$ref": "#/definitions/deprecation"
          }
        },
        "minimumArtifactSchema": {
          "type": "integer",
          "description": "The oldest `analysis.json` schema_version a client must still be able to render. Stored artifacts are served AS WRITTEN — §38 forbids reprocessing that buys nothing — so this is the floor a renderer has to cope with, not a promise that everything is current."
        },
        "currentArtifactSchema": {
          "type": "integer",
          "description": "The schema_version freshly analysed swings are written at."
        }
      }
    },
    "deprecation": {
      "title": "ApiDeprecation",
      "type": "object",
      "required": [
        "version",
        "sunsetOn"
      ],
      "properties": {
        "version": {
          "$ref": "#/definitions/apiVersion"
        },
        "sunsetOn": {
          "type": "string",
          "description": "ISO-8601 date this version stops answering."
        },
        "replacedBy": {
          "$ref": "#/definitions/apiVersion"
        }
      }
    },
    "swingViewSummary": {
      "title": "SwingViewSummary",
      "type": "object",
      "description": "One camera's recording of a swing, as the log and the player need it.",
      "required": [
        "id",
        "view",
        "mediaKey",
        "revision",
        "fps",
        "frameCount",
        "width",
        "height",
        "status",
        "overallScore",
        "band"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "view": {
          "type": "string",
          "enum": [
            "dtl",
            "face_on"
          ]
        },
        "mediaKey": {
          "type": "string",
          "description": "The analyzer's working-directory stem. NOT an address — storage keys are derived from identity, never stored."
        },
        "revision": {
          "type": "integer",
          "description": "Which analysis run's artifacts this view currently addresses. A re-analysis writes r<n+1> and only then repoints the row, so a golfer mid-scrub finishes on what they started with."
        },
        "fps": {
          "type": "number"
        },
        "frameCount": {
          "type": "integer"
        },
        "width": {
          "type": [
            "integer",
            "null"
          ]
        },
        "height": {
          "type": [
            "integer",
            "null"
          ]
        },
        "status": {
          "type": "string"
        },
        "overallScore": {
          "type": [
            "number",
            "null"
          ]
        },
        "band": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "swingSummary": {
      "title": "SwingSummary",
      "type": "object",
      "required": [
        "id",
        "label",
        "referenceLabel",
        "views",
        "primaryViewId",
        "frameCount",
        "fps",
        "view",
        "overallScore",
        "band",
        "scoringModelVersion",
        "status",
        "createdAt",
        "model",
        "tempoRatio",
        "traceEnabled",
        "poseCoverage"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "The swing's own uuid. Since migration 0006 this is NOT a folder name."
        },
        "label": {
          "type": "string",
          "description": "What to call this swing on screen — a uuid is useless to a person, so the human-facing name is explicit."
        },
        "referenceLabel": {
          "type": [
            "string",
            "null"
          ],
          "description": "Set on the bundled model swings; null on a golfer's own."
        },
        "views": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/swingViewSummary"
          }
        },
        "primaryViewId": {
          "type": [
            "string",
            "null"
          ]
        },
        "frameCount": {
          "type": "integer"
        },
        "fps": {
          "type": "number"
        },
        "view": {
          "type": "string"
        },
        "overallScore": {
          "type": [
            "number",
            "null"
          ]
        },
        "band": {
          "type": [
            "string",
            "null"
          ]
        },
        "scoringModelVersion": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "type": "string",
          "description": "Rolled up across views: `ready` only when every view is, `failed` if any failed."
        },
        "createdAt": {
          "type": "integer"
        },
        "model": {
          "type": [
            "string",
            "null"
          ],
          "description": "Display-only extras below are still read from `analysis.json` rather than denormalized onto the swing row. Cheap at today's counts; promoting them to columns is an additive migration, not a rearchitecture."
        },
        "tempoRatio": {
          "type": [
            "number",
            "null"
          ]
        },
        "traceEnabled": {
          "type": "boolean"
        },
        "poseCoverage": {
          "type": "number"
        },
        "sessionId": {
          "type": [
            "string",
            "null"
          ],
          "description": "The practice session this swing belongs to, once a capture flow minted one (D41 additive). Null on every swing recorded before session mode, and on any swing whose session was deleted — the log falls back to inferring a session from time, so the golfer never sees a gap."
        }
      }
    },
    "swingListResponse": {
      "title": "SwingListResponse",
      "type": "object",
      "required": [
        "swings"
      ],
      "properties": {
        "swings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/swingSummary"
          }
        }
      }
    },
    "sessionType": {
      "title": "SessionType",
      "type": "string",
      "enum": [
        "swing_analysis",
        "practice_drills",
        "video_only"
      ],
      "description": "What the golfer came to do. `practice_drills` and `video_only` are quarantined from durable averages — a drill rep is not a scored swing attempt, and a video-only clip was never analysed."
    },
    "sessionSummary": {
      "title": "SessionSummary",
      "type": "object",
      "description": "One practice session. Minted by the capture flow on the FIRST recorded swing, never on entering session mode — a golfer who opens the camera and walks away leaves nothing behind.",
      "required": [
        "id",
        "date",
        "name",
        "sessionType",
        "swingCount",
        "createdAt"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "description": "The calendar day it was recorded on, `YYYY-MM-DD` in the golfer's own timezone as the client stated it."
        },
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "The golfer's chosen name, or null when they never renamed it. Null is not 'unnamed': it is what tells the log to keep its date title rather than print a number the app counted."
        },
        "sessionType": {
          "$ref": "#/definitions/sessionType"
        },
        "swingCount": {
          "type": "integer",
          "description": "How many swings currently point at this session. What locks the type, and what numbers the next default name."
        },
        "createdAt": {
          "type": "integer"
        }
      }
    },
    "sessionListResponse": {
      "title": "SessionListResponse",
      "type": "object",
      "required": [
        "sessions"
      ],
      "properties": {
        "sessions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/sessionSummary"
          }
        }
      }
    },
    "sessionCreateRequest": {
      "title": "SessionCreateRequest",
      "type": "object",
      "description": "POST /api/v1/sessions. Every field optional: a session created with an empty body is today's unnamed analysis session, which is exactly what the capture screen mints when the golfer touched nothing.",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "Send null (or omit) while the title is still the app's own default — see `sessionSummary.name`."
        },
        "sessionType": {
          "$ref": "#/definitions/sessionType"
        },
        "date": {
          "type": "string",
          "description": "`YYYY-MM-DD`, stated by the client because only the phone knows the golfer's timezone. Defaults to the server's date when omitted."
        }
      }
    },
    "sessionPatchRequest": {
      "title": "SessionPatchRequest",
      "type": "object",
      "description": "PATCH /api/v1/sessions/:id. Partial, like the profile patch — a screen sends only what it edits. `sessionType` is rejected once the session has swings.",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ]
        },
        "sessionType": {
          "$ref": "#/definitions/sessionType"
        }
      }
    },
    "sessionResponse": {
      "title": "SessionResponse",
      "type": "object",
      "required": [
        "session"
      ],
      "properties": {
        "session": {
          "$ref": "#/definitions/sessionSummary"
        }
      }
    },
    "headMarker": {
      "title": "HeadMarker",
      "type": "object",
      "description": "A hand-placed club head, normalized 0–1 against the video frame — the same convention as `analysis.json`. Corrections live in the database and merge by frame at render time, never in the artifact, which is rewritten wholesale by every re-analysis.",
      "required": [
        "frame",
        "x",
        "y"
      ],
      "properties": {
        "frame": {
          "type": "integer",
          "minimum": 0
        },
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      }
    },
    "markerListResponse": {
      "title": "MarkerListResponse",
      "type": "object",
      "required": [
        "markers"
      ],
      "properties": {
        "markers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/headMarker"
          }
        }
      }
    },
    "markerSaveRequest": {
      "title": "MarkerSaveRequest",
      "type": "object",
      "description": "A batch, deliberately not idempotent-per-click: a drag emits a position per pointer move and there is nothing to be gained from a request each.",
      "properties": {
        "markers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/headMarker"
          }
        },
        "deleted": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0
          }
        }
      }
    },
    "markerSaveResponse": {
      "title": "MarkerSaveResponse",
      "type": "object",
      "required": [
        "saved",
        "deleted"
      ],
      "properties": {
        "saved": {
          "type": "integer",
          "minimum": 0
        },
        "deleted": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "stageMark": {
      "title": "StageMark",
      "type": "object",
      "description": "A hand-corrected swing stage: which frame this swing's `top` (or `impact`, …) really is.",
      "required": [
        "stage",
        "frame"
      ],
      "properties": {
        "stage": {
          "type": "string",
          "enum": [
            "approach_start",
            "backswing_start",
            "downswing_start",
            "impact",
            "finish_start"
          ]
        },
        "frame": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "stageListResponse": {
      "title": "StageListResponse",
      "type": "object",
      "required": [
        "stages"
      ],
      "properties": {
        "stages": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/stageMark"
          }
        }
      }
    },
    "stageSetRequest": {
      "title": "StageSetRequest",
      "type": "object",
      "description": "One stage per request, unlike the marker batch: picking a stage is a single deliberate choice. `frame: null` clears it.",
      "required": [
        "stage"
      ],
      "properties": {
        "stage": {
          "type": "string"
        },
        "frame": {
          "type": [
            "integer",
            "null"
          ]
        }
      }
    },
    "job": {
      "title": "Job",
      "type": "object",
      "description": "One analyzer run over one clip. A swing with two cameras is two jobs, not one job that quietly does half the work. Job state lives in Postgres, not in a process.",
      "required": [
        "id",
        "viewId",
        "status",
        "stage",
        "progressPct",
        "message",
        "log",
        "startedAt",
        "finishedAt"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "viewId": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "queued",
            "running",
            "done",
            "failed"
          ]
        },
        "stage": {
          "type": "string"
        },
        "progressPct": {
          "type": "number"
        },
        "message": {
          "type": "string"
        },
        "log": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "startedAt": {
          "type": "integer"
        },
        "finishedAt": {
          "type": [
            "integer",
            "null"
          ]
        },
        "runner": {
          "type": "string",
          "enum": [
            "spawn",
            "queue"
          ],
          "description": "Which path runs the job: a child process of the web server (spawn) or the QStash-dispatched hosted worker (queue). Optional and informational — clients render progress identically either way."
        }
      }
    },
    "jobStatusResponse": {
      "title": "JobStatusResponse",
      "description": "Either the job row, or `{ status: 'idle' }` when no run has ever been started for this view.",
      "anyOf": [
        {
          "$ref": "#/definitions/job"
        },
        {
          "title": "JobIdle",
          "type": "object",
          "required": [
            "status"
          ],
          "properties": {
            "status": {
              "type": "string",
              "enum": [
                "idle"
              ]
            }
          }
        }
      ]
    },
    "accountDeletion": {
      "title": "AccountDeletion",
      "type": "object",
      "description": "What DELETE /api/v1/account actually removed. Reported back rather than answered with 204 because a golfer deleting their account is entitled to see the count, and because a partial deletion has to be distinguishable from a complete one — three systems are involved and no transaction spans them.",
      "required": [
        "userId",
        "mediaObjects",
        "swings",
        "views",
        "authIdentityDeleted"
      ],
      "properties": {
        "userId": {
          "type": "string",
          "description": "The account that was deleted. Always the caller — this route cannot name another."
        },
        "mediaObjects": {
          "type": "integer",
          "description": "Objects removed across the source and artifact buckets."
        },
        "swings": {
          "type": "integer"
        },
        "views": {
          "type": "integer",
          "description": "Camera angles removed. A two-camera swing is two views."
        },
        "authIdentityDeleted": {
          "type": "boolean",
          "description": "False only where Supabase is unconfigured (the local development split, D7). The row data is gone either way; a client must not claim a full deletion when this is false."
        }
      }
    },
    "swingDeletion": {
      "title": "SwingDeletion",
      "type": "object",
      "description": "What DELETE /api/v1/swings/:id removed. Reported rather than answered with 204 for the same reason accountDeletion is: media objects and database rows are two systems with no transaction spanning them, so a partial deletion must be distinguishable from a complete one.",
      "required": [
        "swingId",
        "mediaObjects"
      ],
      "properties": {
        "swingId": {
          "type": "string",
          "description": "The swing that was deleted. Always one the caller owns — the route answers 404 rather than touch another account's swing."
        },
        "mediaObjects": {
          "type": "integer",
          "description": "Objects removed across the source and artifact buckets for this swing."
        }
      }
    },
    "notification": {
      "title": "Notification",
      "type": "object",
      "description": "One inbox row (§29 + D55 + D60). Grouped delivery is a property of the ROW, not of a delivery channel: rows sharing an open groupKey collapse server-side into one row whose `count` says how many events it stands for, which is how conversation messages avoid one entry per message.",
      "required": [
        "id",
        "kind",
        "title",
        "body",
        "data",
        "groupKey",
        "count",
        "createdAt",
        "readAt"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "kind": {
          "type": "string",
          "description": "The §29 taxonomy (+ D55 focus-goal events, + D60 lesson/conversation events). An enum so no client or emitter can mint a free-text kind; growing it is additive and shape-lock-legal.",
          "enum": [
            "analysis_ready",
            "coach_request_approved",
            "coach_request_declined",
            "swing_reviewed",
            "coach_comment",
            "coach_annotation",
            "coach_message",
            "coach_plan",
            "subscription_event",
            "goal_assigned",
            "goal_achieved",
            "goal_regressed",
            "lesson_sent",
            "conversation_reply",
            "review_answered",
            "achievement_earned",
            "golfer_request",
            "golfer_swing",
            "golfer_reply",
            "plan_progress",
            "review_requested",
            "student_message",
            "lesson_viewed",
            "drill_done",
            "student_goal_achieved"
          ]
        },
        "title": {
          "type": "string"
        },
        "body": {
          "type": [
            "string",
            "null"
          ]
        },
        "data": {
          "type": "object",
          "description": "The deep-link payload — swingId / sessionId / goalId / conversationId, whatever the kind's screen needs. Kept open on purpose: each kind's emitter and screen agree on its shape, and a new key must never be a schema break."
        },
        "groupKey": {
          "type": [
            "string",
            "null"
          ],
          "description": "Rows sharing this key collapse while unread (e.g. `conversation:<id>`). Null means the event never groups."
        },
        "count": {
          "type": "integer",
          "description": "How many events this row stands for. 1 unless grouped."
        },
        "createdAt": {
          "type": "integer",
          "description": "Epoch ms of the LATEST folded event — a grouped row surfaces at its newest member's time."
        },
        "readAt": {
          "type": [
            "integer",
            "null"
          ],
          "description": "Epoch ms when acked. Null = unread. Set only by the owner via the read route."
        }
      }
    },
    "notificationListResponse": {
      "title": "NotificationListResponse",
      "type": "object",
      "description": "List and unread count travel together — the bell and the inbox are one fetch, never two.",
      "required": [
        "notifications",
        "unreadCount"
      ],
      "properties": {
        "notifications": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/notification"
          }
        },
        "unreadCount": {
          "type": "integer"
        }
      }
    },
    "notificationAckRequest": {
      "title": "NotificationAckRequest",
      "type": "object",
      "description": "Mark notifications read. Ids in the BODY rather than an /:id route on purpose: acks arrive in batches (opening the inbox acks everything visible), and route-auth's [id] rule is swing-shaped.",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The rows to ack. Omit (with all=true) to ack everything unread."
        },
        "all": {
          "type": "boolean",
          "description": "Ack every unread row the caller owns."
        }
      }
    },
    "notificationAckResponse": {
      "title": "NotificationAckResponse",
      "type": "object",
      "required": [
        "acked",
        "unreadCount"
      ],
      "properties": {
        "acked": {
          "type": "integer",
          "description": "Rows actually transitioned unread → read. Re-acking is 0, not an error."
        },
        "unreadCount": {
          "type": "integer",
          "description": "The count AFTER the ack, so the bell never needs a second round trip."
        }
      }
    },
    "handedness": {
      "title": "Handedness",
      "type": "string",
      "enum": [
        "right",
        "left"
      ],
      "description": "Which side the golfer swings from. The one required onboarding answer — every piece of angle math threads through it (§5.4)."
    },
    "profilePublic": {
      "title": "ProfilePublic",
      "type": "object",
      "description": "The half of the profile a directory or a shared swing may show (§5.1). The split is WHICH OBJECT a field is in, not a per-field flag.",
      "required": [
        "displayName",
        "avatarUrl",
        "bio",
        "region"
      ],
      "properties": {
        "displayName": {
          "type": "string"
        },
        "avatarUrl": {
          "type": [
            "string",
            "null"
          ]
        },
        "bio": {
          "type": [
            "string",
            "null"
          ]
        },
        "region": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "golferProfilePrivate": {
      "title": "GolferProfilePrivate",
      "type": "object",
      "description": "The six things the product asks a golfer (Taylor, 2026-08-20): handedness, swing style, handicap, age, driver speed, 7-iron carry. Deliberately tiny — everything else was removed from product, API and database together, and goals moved out of the profile entirely. Every field nullable: required is a property of the onboarding FLOW, never of the schema. No required list on purpose — fields WILL be added, and an addition must never invalidate an older client.",
      "properties": {
        "handedness": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "right",
            "left",
            null
          ]
        },
        "selfReportedStyle": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "sty_01",
            "sty_02",
            "sty_03",
            "sty_04",
            "unsure",
            null
          ],
          "description": "The golfer's SELF-REPORT against the §15.4 taxonomy — a prior, not a verdict. 'unsure' is a real answer, not a null."
        },
        "handicapRange": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "plus",
            "scratch_5",
            "6_10",
            "11_15",
            "16_20",
            "21_28",
            "29_plus",
            null
          ]
        },
        "driverSwingSpeedMph": {
          "type": [
            "number",
            "null"
          ]
        },
        "sevenIronCarryYds": {
          "type": [
            "number",
            "null"
          ]
        },
        "ageRange": {
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "under_18",
            "18_29",
            "30_39",
            "40_49",
            "50_59",
            "60_69",
            "70_plus",
            null
          ]
        },
        "onboardingCompletedAt": {
          "type": [
            "string",
            "null"
          ],
          "description": "Null means onboarding is still resumable — the profile row itself is the draft (§4.4)."
        }
      }
    },
    "profileResponse": {
      "title": "ProfileResponse",
      "type": "object",
      "description": "GET /api/v1/profile — the caller's own profile. Also the body PATCH answers with, so a client reconciles against what the server confirmed rather than what it sent.",
      "required": [
        "public",
        "private"
      ],
      "properties": {
        "public": {
          "$ref": "#/definitions/profilePublic"
        },
        "private": {
          "$ref": "#/definitions/golferProfilePrivate"
        }
      }
    },
    "profilePatchRequest": {
      "title": "ProfilePatchRequest",
      "type": "object",
      "description": "PATCH /api/v1/profile. Partial on purpose — a screen sends only what it edits, so a screen written before a field existed can never erase it.",
      "properties": {
        "public": {
          "type": "object",
          "properties": {
            "displayName": {
              "type": "string"
            },
            "avatarUrl": {
              "type": [
                "string",
                "null"
              ]
            },
            "bio": {
              "type": [
                "string",
                "null"
              ]
            },
            "region": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        },
        "private": {
          "$ref": "#/definitions/golferProfilePrivate"
        },
        "completeOnboarding": {
          "type": "boolean",
          "description": "Stamps onboardingCompletedAt. Idempotent."
        }
      }
    },
    "rolesResponse": {
      "title": "RolesResponse",
      "type": "object",
      "description": "GET/POST /api/v1/roles — which roles the caller holds and which they may claim (§4.4, D32).",
      "required": [
        "roles",
        "claimable"
      ],
      "properties": {
        "roles": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "claimable": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}
