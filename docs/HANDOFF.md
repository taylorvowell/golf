# Handoff Register — what needs a human, and what already got one

Every task that needs **Taylor** rather than Claude lives here, and **only** here.

`scripts/env-probe.mjs` reads this file at session start and prints every `OPEN` row into the
session, so an outstanding human task arrives in context the same way the phone's LAN address
does. That is the point of the file: a hand-off that lives in prose gets re-asked, and a task
that was already done gets asked for a second time. Both have happened.

## Rules

1. **Never ask Taylor for anything that is not an `OPEN` row here.** Before writing any hand-off
   sentence, check this table. If the ask is not a row, add it as a row first — the act of adding
   it is what forces the check for whether it is already `DONE`.
2. **Never invent a row for something Claude can do.** Money, hardware, an interactive login, a
   vendor dashboard, deleting user data, a production deploy. Everything else is Claude's job —
   "run this command" is not a hand-off.
3. **When Taylor says he did something, mark it `DONE` in the same turn**, with the date and what
   changed. Do not leave it in chat.
4. **A `DONE` row is never deleted.** It is the answer to "did we already do this?" — the exact
   question that gets asked wrong. Rows are only removed when the whole area is retired.

## Status values

`OPEN` — needed now, printed at session start · `BLOCKED` — needed, but waiting on something else
first (state what) · `DONE <date>` — completed, keep for the record · `DROPPED` — no longer needed

<!-- PROBE-READS-BELOW — env-probe.mjs parses the table rows under this marker -->

| Status | What | Why it needs a human | Notes |
|---|---|---|---|
| DONE 2026-08-11 | Google OAuth **web** client created | Google Cloud Console — interactive login | `665583572860-v7lsnd65s2pmr9g4qu29jurb7gqoltk4.apps.googleusercontent.com`. This is the id the app passes to `GoogleSignin.configure`. |
| DONE 2026-08-11 | Google OAuth **Android** client created | Google Cloud Console — interactive login | `665583572860-tlfq1jrit3g4hrmoatlcub81r6t13gn8.apps.googleusercontent.com`, bound to `com.swingsage.spike` + SHA-1 `5E:8F:…:F6:25`. Must exist; never appears in the bundle. |
| DONE 2026-08-11 | Google provider enabled in Supabase, with Authorized Client IDs | Supabase dashboard — interactive | Web client id + secret in the provider; both client ids in the authorized list. |
| DONE 2026-08-11 | `apps/mobile/.env` populated | Holds real (public) values; gitignored | 5 `EXPO_PUBLIC_*` keys set. `.env.example` carries the same public ones. |
| DONE 2026-08-11 | Android wireless debugging paired | Physical device interaction | Pairing survives reboots; the **port changes** and must be read off the phone each time. |
| DONE 2026-08-22 | Supply the three AI-coach portraits | Art/likeness sourcing — a taste and rights call, not a command | Overwrite `apps/mobile/assets/coaches/mark.png`, `sean.png`, `julie.png` with the real portraits — same paths, same names, no code change. Square, **512×512 or larger**, PNG, face centred (they are circle-cropped at 40–52 pt). Delivered as `.jpg` on 2026-08-22; the roster requires those paths. Personas: Mark = Gemini voice `Orus`, Sean = `Charon`, Julie = `Zephyr`. |
| DONE | Fix the `ANDROID_SDK_ROOT` user environment variable | Windows user env vars — GUI | Its value contains its own name, so every Android build needs the inline override. Settings → System → About → Advanced system settings → Environment Variables → User → `ANDROID_SDK_ROOT` = `C:\Users\taylo\AppData\Local\Android\Sdk` |
| BLOCKED | Rename the Android package `com.swingsage.spike` → `com.swingsage.app` | Needs a new Android OAuth client first (Console, interactive) | Permanent from the first store upload; visible in the Play Store URL forever. **Blocked on:** one additive Android client for the new package on the same SHA-1. Free. Claude does the rename once it exists. |
| BLOCKED | Apple Developer Program enrolment ($99/yr) | Money | Needed for Sign in with Apple and any iOS build. Nothing iOS has ever been compiled — there is no Mac. |
| BLOCKED | Create the IAP products in App Store Connect + Play Console | Vendor dashboards, and each product's price/ID must be registered before StoreKit or Play Billing returns anything | Seven SKUs now, ids in `apps/mobile/src/features/billing/storeProducts.ts`: `com.swingsage.app.pro.monthly` $16.99, `com.swingsage.app.pro.annual` $119.99, `com.swingsage.app.topup.50` $9.99 (consumable), plus a 21-day intro trial on both Pro subscriptions — and the four instructor-membership subscriptions (2026-08-26): `com.swingsage.app.instructor.gold.monthly|annual` and `.platinum.monthly|annual`, prices TBD (billing-iap; also blocked on the student-seats decision row). **All six subscriptions must be created in ONE iOS subscription group ranked pro < gold < platinum** so membership upgrades prorate natively; no trial on any instructor SKU. Also enrol in the **Small Business Program** (Apple) / **15% first-$1M** (Google) — it is the difference between 65% and 58% margin on Pro. **Blocked on:** Apple Developer Program enrolment and the `com.swingsage.app` package rename, both above. |
| DONE 2026-08-22 | A2P 10DLC registration — **not applicable, closed** | Superseded by the provider choice | Never needed. Taylor set up **Twilio Verify** (not Programmable SMS) on `golf-swing`; Twilio exempts verification-only traffic from A2P 10DLC, so the business-identity gate that blocked this row for ten days does not exist on this path. Verified live: `phone: true`, `sms_provider: "twilio_verify"`. This also lifts the D46 hold on phone OTP. |
| DONE 2026-08-22 | Add your mobile number to `AUTH_ALLOWED_PHONES` | Personal data — Claude would not guess a phone number | Taylor gave the number; Claude set it in `apps/web/.env` (gitignored). He also registered it as a **Test OTP** number in the Supabase dashboard, so the dev flow sends no SMS and costs nothing — see `ENVIRONMENT.md`, which had claimed a hosted project could not do that. |
| OPEN (optional, quality-of-life) | One more Google OAuth **Android** client, for a side-by-side DEBUG app | Google Cloud Console — interactive login | Today the dev build and the portable release build are the same package, so installing one replaces the other (one command each way, data and sign-in survive — RUNBOOK "Two modes on the phone"). If you want BOTH on the phone at once — "SwingSage" for the simulator and "SwingSage Dev" for desk work — the debug variant needs `applicationIdSuffix .debug`, and native Google sign-in then needs an Android client bound to `com.swingsage.spike.debug` on the SAME debug SHA-1 (`5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25`). Free, additive, breaks nothing. Say the word and Claude wires the suffix + a distinct app name/icon. Not needed for anything to work — pure convenience. |
| OPEN | Turn on Twilio Verify for the **production** project `swingsage-prod` | Dashboard + the same Twilio credentials | `golf-swing` (dev) is live; production still reads `phone: false`, `sms_provider: "twilio"` — the unset pair. Same three fields, same Twilio account: https://supabase.com/dashboard/project/nprxxjeavdlsqthnofof/auth/providers → **Phone** → provider **Twilio Verify** → Account SID, Auth Token, Verify Service SID → enable. Reuse the one Verify Service; a second is not needed. Not urgent — nothing ships off production auth until the store build. |
| OPEN | **Auth config on `golf-swing` — email codes + web Google sign-in** | Supabase + Google Cloud dashboards — no management token exists locally and the MCP has no auth-config tool | Email sign-in on the deployed site is currently **impossible**: the sign-in screen asks for a six-digit code, but the email still carries the default magic link, and that link redirects to `localhost:3000` (dev-default Site URL). One sitting, ~5 min, four stops. **(1) Email templates** — https://supabase.com/dashboard/project/xjcjqwcmwoouxczrrvar/auth/templates — put the six-digit code in **three** templates (the app verifies a typed code, never a link): **Magic Link** (existing email user signing in) and **Confirm signup** (new email user's first code) get e.g. `<p>Your SwingSage sign-in code is {{ .Token }}. It expires in one hour.</p>`; **Change Email Address** (the app's add-your-email onboarding step) gets `<p>Your SwingSage confirmation code is {{ .Token }}.</p>`. **(2) Site URL** — https://supabase.com/dashboard/project/xjcjqwcmwoouxczrrvar/auth/url-configuration → `https://www.swingsage.io`. **(3) Redirect URLs** (same page) — add all four, needed by the new web "Continue with Google": `https://www.swingsage.io/auth/callback`, `https://golf-pi-eight.vercel.app/auth/callback`, `http://localhost:3000/auth/callback`, `http://127.0.0.1:3000/auth/callback`. **(4) Google Cloud Console** — the OAuth **web** client (`665583572860-v7lsnd…`, APIs & Services → Credentials) → **Authorized redirect URIs** → add `https://xjcjqwcmwoouxczrrvar.supabase.co/auth/v1/callback`. It was created for the phone's native sign-in, which never redirects, so this URI is almost certainly missing — without it web Google sign-in dies at Google with `redirect_uri_mismatch`. None of this breaks local dev or the phone's native Google path. **The whole set must be replicated on `swingsage-prod` at the auth cutover** (noted in that row; the Google URI becomes `https://nprxxjeavdlsqthnofof.supabase.co/auth/v1/callback`). |
| OPEN | Delete the two `jars-*` Supabase projects | Dashboard — the API cannot do it | Taylor, 2026-08-22: he wants them gone. **Claude cannot do this** — the Supabase MCP exposes `pause_project`/`restore_project` but no project delete; Supabase gates it behind a typed confirmation in the dashboard. Two links, ~30 seconds each: **`jars-application`** → https://supabase.com/dashboard/project/mwicmqgvwcevoyoikunc/settings/general and **`jars-spac`** → https://supabase.com/dashboard/project/ezwsyjvilpcxnantpujj/settings/general — scroll to **Delete project**, type the project name, confirm. Both are `INACTIVE` (paused), us-east-1, PG15, and belong to a different product. **Permanent — there is no undo and no backup on Free.** Neither blocks anything: paused projects do not count against Free's 2-active-project cap, so this is tidiness, not capacity. |
| BLOCKED | Third Supabase project (preview) | Money — Supabase Pro $25/mo | **Revised 2026-08-22.** Free allows **2 active projects**, so dev (`golf-swing`) + production (`swingsage-prod`) both fit on Free — the production project is created in the production-stack row below and needs no money. **The preview project is the third, and it is the thing that forces Pro.** Three environments is a recorded decision that stays unmet until then. Not urgent: it lands with the launch-prep upgrade, alongside Vercel Pro. |
| DONE 2026-08-12 | Seek exactness confirmed on the S25+ | Physical device interaction | Taylor: *"seek looks good."* The figure directly observed was **30 seeks · 100.0% exact · p95 0 · max 0**; the larger run was confirmed but not read back, so `CURRENT-STATE.md` §11b claims only n=30 and names the shortfall. `mobile-player` step 01 closed on this. Re-run any time: RUNBOOK §11. |
| DONE 2026-08-13 | Add a Google account to the desktop emulator | Credential | This is the **only** thing standing between Claude and verifying UI on its own — with it, the emulator replaces most "please look at your phone" asks permanently. The `swingsage` AVD is built, tuned and has the app installed and talking to Metro; it stops at Google's *"Checking info…"* because a fresh AVD has no Google account, so everything behind the auth gate (swing log, player, overlays, Analysis, Compare) is unreachable on it. **Start it with `~/AppData/Local/Android/Sdk/emulator/emulator.exe -avd swingsage -gpu host` (or ask Claude to), then in the emulator: Settings → Passwords & accounts → Add account → Google.** It persists in the AVD across reboots and reinstalls. Any Google account works — it does not have to be the one on your phone, as long as its email is in `AUTH_ALLOWED_EMAILS`. Signed in as `taylorvowell@gmail.com`; **verified to survive a full emulator kill and relaunch**, so Claude can restart it unattended. RUNBOOK §13. |
| DONE 2026-08-22 | Decide the analyzer worker host | Strategic + spend | **ANSWERED: Modal** (D64). Serverless GPU with scale-to-zero and per-second billing is the honest shape for a worker that runs ~76s a few times an hour and is idle the rest — a rented GPU VM bills 24/7 for ~2% utilisation. The measurement that priced it: pose is **2.32x** faster on CUDA (70.4 -> 30.4 ms/frame) on a 2016 GTX 1080, output agreeing to 0.94 px, so a GPU host does not change the analysis. The bigger lever was not hardware at all — a development-only stage (`variants`, eight club re-solves) was **72%** of a run, and turning it off in production takes a job from 273s to ~76s. Railway is struck: no GPU, idle-billed, and a second compute vendor for nothing the design needs. Full breakdown: `.claude/architecture/swing-analysis-speed-2026-08-18.md`. Signup folds into the production-stack row below. |
| DECLINED 2026-08-13 | Read the overlay's frame-lock off the S25+ | Physical device interaction | Taylor: *"I don't want to measure. skip this."* Declined while **available**, not while blocked — the phone was connected (`10.0.1.123:39593`) and `adb shell input` was landing again. `mobile-player` step 02 closed carrying it as a named shortfall, and **the Skia question (D23/D36) stays open on it — see D51**. Do **not** re-ask; if the trace ever looks like it stutters, this is the number that settles it: RUNBOOK §12b, about a minute. |
| OPEN | Design verdict on the swing screen + the two-decoder reading | Design judgement + a phone-only measurement | **Both features are now verified to WORK** — Claude drove the desktop emulator and confirmed the Analysis panel renders and its taps seek to the right frame, and that side-by-side comparison holds two swings at the same position while scrubbing. What is left is genuinely yours: **(a)** whether the full-bleed screen, the scrubber, the overlay tiles, the Analysis panel and the side-by-side layout *look right* — taste, not correctness; **(b)** the one number the emulator cannot produce, because it is software-rendered: open a swing on the **S25+**, turn on Compare, and read **Overlay drift** in METRICS with two videos running. If it holds, `mobile-player` step 04's open question is closed. |
| DONE 2026-08-17 | Reinstall the dev build on the S25+ (adds `react-native-svg`, light/dark theme, `expo-font` + `expo-linear-gradient` + the Ideal Swing palette) | Physical device interaction | **Done by Claude the moment Taylor re-enabled wireless debugging**: clean prebuild + `expo run:android --device SM_S936U1` over wifi, installed and verified running — the step-09 UI renders on the phone (dark via "match my phone", which this rebuild's `userInterfaceStyle: automatic` fixed). One trap hit and recorded in ENVIRONMENT.md: Metro had silently hung (accepted TCP, served nothing — the dev client showed ECONNREFUSED), which mimics a firewall problem; restarting Metro fixed it. Original ask: Three changes are stale on your phone's dev client: the after-swing gauges added the app's first SVG native module (crashes the after-swing screen until reinstalled), the theme system flipped `userInterfaceStyle` `dark` → `automatic` — until the rebuild, the OS always reports dark, so the new light-default only appears via Settings → Appearance → Light, never from "Match my phone" — and design-system step 01 (2026-08-14) added `expo-font`/`expo-linear-gradient` (two more native modules) plus the new navy/cobalt/aqua palette and bundled fonts. Because native config and modules changed, the rebuild needs a clean prebuild: `cd apps/mobile && npx expo prebuild -p android --clean && npx expo run:android --device` — or just ask Claude with the phone connected. The desktop emulator was rebuilt with all of this 2026-08-14. |
| DONE 2026-08-14 | Brand wordmark verdict: "Ideal Swing" (the mockups) vs "SwingSage" (the app today) | Product naming — judgement call | **Settled by Taylor providing the real logo the same day** — its wordmark reads *Swingsage*, so the brand is **SwingSage** and every "Ideal Swing" string in the mockups is replaced. Master lockup (mark + wordmark, white-on-dark, aqua `#42CBCE`/`#3FFFF5` swoosh) saved at `apps/mobile/assets/brand/swingsage-logo.svg`; the design system renders it via a `BrandLogo`/`BrandMark` component (design-system step 02). |
| OPEN | **Device pass of the rebuilt side-by-side comparison on the S25+** | Two video decoders on one screen is the one thing the emulator provably cannot verify (`react-native.md`: two software-decoded surfaces plus a long clip ANRs it) | Swing view → the compare orb → pick **Pro Swing** (the only reference swing in the database). Three things to judge, all of them yours: **(1)** the picker collapses and two columns appear; **(2)** scrub anywhere and both golfers are at the same point in the swing — at the top, halfway down, at impact; **(3)** the follower keeps up with the finger without visible lag. Both panes now crop to the golfer, so they should be roughly twice the size they were and the same size as each other. Automated coverage is green (58 suites, 536 tests) and the alignment math is pinned against the real fixture tables, but nothing here measures follower seek cost on real silicon. |
| OPEN | Device pass of the rebuilt Swing Report (video-open) on the S25+ | Physical device interaction + a phone-only measurement | Design-system step 07 made the swing screen the mockup's sheet-over-video: the report sheet rides over the **live player**, dragging it down enters **video-open** (player controls slide in, pill nav out, sheet drops away), with a phase-labelled scrub. Verified working end-to-end on the emulator, which cannot measure: on the S25+, open a swing from the log, scroll into video-open, scrub, and confirm frame-lock/seek feel (the real numbers, if wanted, are RUNBOOK §11/§12b on the after-swing player — same transport). Needs the reinstall row above first (same dev client, no new native modules). This is step 07's named device-pass shortfall. |
| DONE 2026-08-17 | Replicate API token in the voice-bank generation script's env | Credential — Taylor's existing Replicate account | Taylor provided it (originally into `apps/mobile/.env`; Claude moved it to **`apps/web/.env`** — server-side, gitignored, probe-reported — since the consumer is the server-side D57 generation script, never the app). env-probe now prints `REPLICATE_API_TOKEN=set`. Supersedes the earlier Google AI Studio key ask: the chosen model runs on Replicate directly (`replicate.com/google/gemini-3.1-flash-tts`). Vendor call + addendum in `.claude/architecture/voice-tts-vendor-2026-08-14.md`. |
| OPEN | Walk session mode and iterate to sign-off (track step 03 — YOUR gate) | Design judgement — you mandated this gate (D61) | **This is now the ONLY incomplete step in the whole track — everything else is built and green.** Rewritten 2026-08-26: the loop is no longer a stub, and it is no longer a *session*. Record button → camera (real 1080p240 preview, upload door top right, a status chip top left when a swing is still analysing) → Record → mark the strike on the filmstrip → Save → the after-swing screen plays your trimmed clip while the analyzing bar walks the REAL job stages, then the same screen swaps to the served report with markers and a score. The bar under it is **Done · Swings · ⏺ Record New · Delete · Favorite**. Things to judge: **(a)** does **Done** read right as the way out now that nothing 'ends' — you leave and land on your log, which is simply already correct (the old 'Saving session…' card is gone); **(b)** back on the after-swing screen goes to the camera with no question asked, and back on the camera leaves — is that what your thumb expects; **(c)** with **Video replay** off (cog → settings) a save keeps you on the camera and the only report is the small chip top-left — is that enough to trust that your swing is being worked on; **(d)** the star is greyed out until the swing has actually uploaded, on purpose. Say what reads wrong, iterate, then say "signed off". |
| OPEN | S25+ design pass over the FINISHED app — every screen, both themes | Design judgement + physical device | Design-system step 09 (2026-08-17) put the last screens on the system and deleted the legacy look: Home (performance-card hero), Coach, Goals, Settings, Profile, Record, sign-in, delete-account, error/upgrade states — plus the player re-tokened from acid-green onto aqua/cobalt. All verified on the emulator in light AND dark (screenshots in the step 09 progress entry); what is left is taste on real glass: walk every screen in both themes and say what reads wrong. Needs the reinstall row above first (no NEW native modules since — the same rebuilt dev client covers this). Subsumes the per-screen look-check halves of the two rows above; their measurement halves (overlay drift, video-open scrub feel) stand. |
| OPEN | A second physical Android device for the dual-device spike | Hardware + spend | `dual-device-spike` steps 02-03 measure whether two phones can be triggered together and aligned to within a frame. **Steps 01-02 are developable with your S25+ and a browser tab on this PC** - a dev-only `/dev/pair` page acts as the second peer in either role, connects DIRECTLY to the phone over the LAN (no relay), and exposes every ICE candidate via `chrome://webrtc-internals`. The emulator also works as a peer but needs a `coturn` relay in Docker, because the emulator NAT blocks any direct connection to a phone. Step 03 is the one that genuinely needs a second phone: the emulator is a synthetic camera feed on x86_64, so no timing, fps or alignment number from it means anything, and the whole point of the spike is a timing number. It does not need to be new or good - any Android that can record 1080p is a valid second camera, and a mid-range device is arguably the more honest test, since the capability negotiation exists precisely because not every phone is an S25+. A borrowed phone for an afternoon closes it. Until then step 03 cannot start and the §12 differentiator stays unproven. |
| OPEN | Walk the coach-surface UI and iterate to sign-off (track step 05 — YOUR gate) | Design judgement — same gate shape as session-mode's D61 | Built 2026-08-19, all stubs, no wiring: **Coach tab** rebuilt as the AI coach (hero, Top tip + top drill, guided stance-analysis door, four impact-ranked focus areas with personal scores — the featured one draws its overlay on a stance stage — and four matched drills) · **instructor presence** (DEBUG → Instructor → "Has local instructor" toggles the face bubble bottom-right on Coach and swaps the profile drawer between the connected card and the find-an-instructor door; both open placeholder Instructor/chat pages) · **guided stance analysis** (auto-advancing draw→hold→clear walkthrough, DTL then front view, narration standing in for the voice; entered from the Coach card or the home highlight, which hides ONLY via its X — walking it leaves the card up; DTL beats draw on the golfer's own P1 address photo with anchors from their artifact's keypoints + detected shaft, face-on beats fall back to pose art until a face-on artifact exists; DEBUG → Coach has “Reset stance intro” and “Stance: pose art”) · **Progress** now matches `.claude/SAMPLE-progress-page.html` exactly (Before/Now bars, +9/+6/+11 deltas, coach-voice description, confidence chip — canned at the placeholder seam). Walk it in both themes with the instructor flag on and off; say what reads wrong, iterate, then say "signed off". Stance-stage annotation anchors are eyeballed and expected to need tuning here. |
| OPEN | Walk the upgrade UI and iterate to sign-off | Design judgement — YOUR gate | **Profile drawer** → the Pro card (the CTA), and Settings → **Plan**. The debug sheet's **Subscription state** group jumps between all seven states (trial day 1 / ending, Pro plenty / low / spent, Free never / expired); **Upgrade moments** opens the two refusal sheets. Screens: Upgrade, Subscription, the allowance meter. Nothing charges — the button is where the native App Store / Play sheet will open. |
| OPEN | Club-trace winner verdict across all 10 swings — pick the solution, then Claude deletes the rest | Design/quality judgement — this is the goal: finalize the choice and remove everything else | Everything is staged (2026-08-19): all 10 fixture swings sit in ONE session dated today at the top of the swing log (plus 3 sample sessions in the past for log formatting), and the app-wide DEBUG menu has a **"Club solution"** chip row on any swing report (one chip per stored solve + `primary`, each with its swing-coverage %, picked one marked ●; picking forces the club + trace overlays on). All 10 were RE-ANALYSED and republished the same day with **19 variants each** — the original 13 plus the START/BOTTOM anchor dimension: impact anchored to the Address landmark, impact anchored to the detected ball, and hand-based start (takeaway refine off), each also paired with the incumbent moving-average trace. The trace STYLING verdict is already settled (gradient3d·bulge·aquaDeep·silk, decisions/mobile-client.md) — this row is only the SOLVE/trace-method verdict. The incumbent is `model_traj_moving` (the 2026-08-08 pick, drawn by default). Walk the swings, flip between solutions, and name the winner; remember `swing1`'s sparse downswing is a detector result, not a trace fault — the pretty `primary` line there is 24 fabricated points. THE TOOL IS THE TRACEBOARD, AND NOW THE ONLY ONE (services/analyzer/out/traceboard.html): 10 swings x 36 solutions x 9 smoothings, arrow keys to flip, pin-to-compare; white dots = the real detections each solution kept. The 36 include the 2026-08-19 method sweep: anchors, strict, robust, Viterbi global path (+loose), shaft-first (stick-ray heads), ROI cascade (re-detected gap frames), and the CoTracker3 point-tracking spike. **The phone-side lab is GONE** — Taylor removed the flask orb, the `VariantLab` panel and the `VariantGrid` debug page on 2026-08-22 ("we replaced the need for that"); the player now always draws the production pick, and the club trace is OFF by default until this verdict lands. Judge on the traceboard, not on the phone. On your verdict Claude finalizes: default it everywhere, strip the losing variants from the pipeline's VARIANTS/TRACE_MODES lists (making production runs dramatically cheaper — variants is 72% of a run), and record the decision. |
| OPEN | Pair the Autumn tech Bluetooth shutter remote with the S25+ and try it in session mode | Physical device + the remote is in your hands | Built + emulator-verified 2026-08-20: on the session screen a press starts/stops recording (idle → arm, countdown → cancel, recording → stop, post-swing → next swing armed), and the phone's own volume rocker does the same. Needs a native rebuild on the phone first (`pnpm --filter mobile phone:native`, phone connected — new `modules/shutter-remote`). Pair the remote in Android Bluetooth settings like any accessory, open session mode, click. Listen for the camera's record start/stop beeps too (Settings → Recording toggles them). If the remote's second button (some have iOS/Android buttons) does nothing, say so — it may send a keycode outside the claimed set. |
| DONE 2026-08-21 | Record a real swing on the S25+ — the full record → review → save chain | Physical device interaction | **Recording works: 1920x1080 at 240 fps WITH the preview live through the take**, confirmed in logcat (`take RUNNING on rung 0 (240-240 +preview)`). The blocker was a documented API rule this code was violating — a high-speed session carrying both preview and recorder REQUIRES the fixed `[240,240]` range, and the S25+ publishes both `[30,240]` and `[240,240]` with the same upper bound, so the code was tie-breaking onto the invalid one at random; this HAL hangs on an invalid combination instead of refusing it. Capture is now a ladder of real configurations, asked of the device. Four other bugs fell out on the way (main-thread MediaRecorder froze the UI, a missing `stopRepeating` blocked session creation for 11 s, the watchdog was queued behind the call it guarded, and app.json→manifest drift shipped a build with no mic permission). Full write-up in `.claude/feature-tracks/session-mode/_PROGRESS.md`. |
| OPEN | Outdoor pass on the S25+ — a bucket of balls, not a living-room swing | Physical device + a real range | Recording and review work; what has NOT been tested is the part only a range can answer. **(a)** Does the audio seed land near the actual strike outdoors, with wind and neighbouring golfers? This got materially better on 2026-08-22 — the detector is now `swish`, which keys on the club's audible swing-up as well as the click, and it is the first method with a measured score (5/5 on the five hand-labelled raw takes, where the previously shipped `hf` managed 3/5 and once seeded four seconds into the walk back). **But all five of those clips are indoors in a simulator bay, and every impostor it was built to reject — wind, a neighbouring golfer's strike, a ball dropping on turf — only exists outdoors.** This row is the only test of that. Also worth noting: the review track now draws a three-colour swing shape around the mark, so a wrong seed should be obvious at a glance — the shape will not line up with the pictures. **(b)** Does capture stay reliable back-to-back over 20+ takes, and how hot does the phone get (1080p240 is the heaviest thing this app does). **(c)** Is the 5-second clip long enough to contain your full swing now that the window narrowed. Say what breaks and I will tune it. |
| OPEN | Bitrate sweep — the encoder constants are still a guess | Judgement on measured output | `BPP_AT_30 = 0.15` and the `sqrt(fps/30)` scaling in `HighSpeedCameraView.kt` are provisional and have never been measured. The honest test is recording one swing across a bitrate sweep and diffing pipeline output with `scripts/compare_analysis.py`, **watching club coverage** — it degrades long before pose does. Until then a too-low bitrate would show up as a club trace that gets worse, not as a video that looks bad. Not urgent; do it before launch. | Wired 2026-08-20/21 per the capture spec (`.claude/golf_swing_capture_spec/`): Record drives the real Camera2 high-speed session — **1080p at 240 fps with the preview live through the take**, confirmed in logcat — with a **30 s** hard cap and a countdown over the last **5 s**. The take then opens a review screen where you **mark where you hit the ball** on a filmstrip (paused frame, scrub moves the frame); Save cuts a 5 s clip around your mark and the post-swing screen plays it, Delete asks first. Needs a native rebuild (`pnpm --filter mobile phone:native`). What only the phone can answer is now its own row below (the outdoor pass). |
| OPEN | Walk onboarding + My profile on the S25+ (or emulator) and iterate to sign-off | Design judgement — YOUR gate | Built 2026-08-20, fully wired to `/api/v1/profile` and `/roles` (real saves, resumable): **DEBUG → Profile → "Run onboarding"** opens the signup sequence (role → handedness → style → handicap; handedness is the only unskippable answer; it also auto-opens after a fresh signup while unfinished) · **Profile drawer → My profile** — one page, SIX tiles in two columns (handedness, swing style, handicap, age, driver speed, 7-iron carry) with sheet editors (tap a choice to set it, tap the set value to clear; numbers are hold-to-repeat steppers); tap the identity card to edit name/region. Setting handedness to LEFT should flip the capture guide art + view-switcher icons and move zoom/camera-flip to the right rail in session mode. Say what reads wrong, iterate, then "signed off". |
| DONE 2026-08-23 | **Stand up the production stack** — the vendor signups | Money + interactive vendor logins | Taylor signed up for all six vendors and filled `production-credentials.local.txt` (2026-08-22, verified against each vendor — see `docs/ENVIRONMENT.md`). Claude's half is landing: **the analyzer worker is DEPLOYED on Modal and proven end-to-end against production QStash (2026-08-23**, analyzer-service step 07 — app `swingsage-analyzer`, endpoint + capacity numbers in ENVIRONMENT.md/decisions). What remains is engineering, not asks: production migrations + Vercel env + the release pipeline are platform-foundation step 10. The package rename + EAS signing identity keep their own BLOCKED row. The pre-R2-media caveat stands: until `media-pipeline` lands, a range swing is a ~300MB upload — shoot on wifi. |
| OPEN | Upgrade Supabase (Pro $25/mo) and Vercel (Pro $20/mo) **before the store listing goes live** | Money — deliberate future trigger, not needed today | Both upgrades are in-place (no migration, no key changes). Supabase Pro also unblocks the third (preview) project and daily backups; Vercel enforces its commercial-use line by suspending the project, so upgrade BEFORE listing, never after. Triggers restated from the (now DONE) production-stack row so they survive it. |
| OPEN | **Run one swing through the hosted stack on the phone** — sign in, import or record, watch it analyze with the PC out of the loop | Physical device interaction — the last unproven link (Vercel→QStash→Modal→Vercel with real data) | **The install half is DONE 2026-08-23**: Taylor re-enabled wireless debugging, the sweep found the phone at a fresh port, and the release APK (hosted API baked in, no Metro) installed over the dev build — same debug-keystore signature, so app data and Google sign-in survived — and is running on MainActivity. What remains is the swing itself. | The APK is built and waiting (`apps/mobile/android/app/build/outputs/apk/release/app-release.apk`, 94MB, universal, `https://golf-pi-eight.vercel.app` baked in, no Metro needed — verified in the bundle). The entire hosted stack behind it is deployed and verified: www.swingsage.io API live (200/401/426 all correct), swingsage-prod migrated, R2 media, Modal worker. Once connected say "phone's on" — Claude installs and relaunches; then YOU: sign in with Google, import or record a swing, watch the analysis arrive with the PC turned off if you like. That first swing is the one remaining unproven link (Vercel→QStash→Modal→Vercel with real data). |
| OPEN | Move the deployed app's AUTH home onto `swingsage-prod` — two dashboard visits, do it while your test account is the only data | Interactive vendor dashboards | **The cutover migration must also RE-ADD `users_id_auth_users_fk`** — migration 0019 (2026-08-23) dropped it on real-auth databases because under the split a golfer's uid exists only in the auth project, and the FK 500'd every authenticated route on the first phone session. Once auth and data share `swingsage-prod`, the constraint becomes true again and comes back. Also replicate the two `golf-swing` email settings (OTP template + Site URL) per the email row above. | The deployed stack currently authenticates against `golf-swing` (where Google + Twilio already work) while its data lives in `swingsage-prod` — deliberate interim, recorded in `decisions/platform-data.md`. The cutover: (1) `swingsage-prod` dashboard → Auth → Providers → Google: paste the SAME web client id + secret `golf-swing` uses (Google Cloud side needs nothing — same client works for both). (2) The already-open Twilio Verify row on `swingsage-prod`, same sitting. (3) The full auth-config set from the "Auth config on `golf-swing`" row, on `swingsage-prod` this time: the three `{{ .Token }}` email templates, Site URL, the four `/auth/callback` redirect URLs, and the Google client's `https://nprxxjeavdlsqthnofof.supabase.co/auth/v1/callback` redirect URI. Then say "done" and Claude flips the four Vercel auth vars and re-creates your account. **The clock on this row:** `users.id` IS the auth uid, so accounts re-key on cutover — free while the only account is yours, painful after real golfers exist. | Taylor, 2026-08-22: *"we need modal if we are wanting to go to production yes? I do not want half measures or MVP locally... I like to build infrastructure once."* **That is now the decision — see D64.** An earlier suggestion in the same conversation (host the API but tunnel to the analyzer on this PC's GTX 1080) is WITHDRAWN: it was exactly the interim build the standing infrastructure constraint exists to forbid. **Everything you need to paste is in the gitignored `production-credentials.local.txt` at the repo root** — one file, annotated with the exact dashboard path for every value. Fill it in and say "done"; Claude does the rest and no secret is typed into a dashboard by hand. **Sign up for six:** Supabase **Free for now** (revised 2026-08-22 — once media moved to R2, Pro buys only no-idle-pause, daily backups and a third project; none binds before launch, and the upgrade is in-place with no migration. **Upgrade to Pro $25/mo when you want the preview environment, or the first real golfer's swings land.**) · Vercel **Hobby, free** (revised 2026-08-22 — Hobby's bar is *revenue*: ads, payments, client work, a monetized product. A pre-launch project with no store listing and no payment flow is none of those, and its caps are far above solo-dev volume. **Upgrade to Pro $20/mo BEFORE the store listing goes live** — Vercel enforces by suspending the project, and the upgrade is in-place.) · **Modal** (the analyzer worker; serverless GPU, scale-to-zero, ~$0.02/swing at L4) · **Upstash QStash** (pay-as-you-go, ~$0 at this volume) · **Cloudflare** (free + a ~$12/yr domain, at cost) · **Expo/EAS** (free account, but used from the FIRST build so the signing SHA-1 never changes). **$0/mo fixed today** — every vendor is on a free tier. Just ~$12/yr for the domain, ~$20 of Anthropic credit, and ~$0.02 per swing. **~$45/mo at launch**, when Supabase Pro and Vercel Pro land together. **Two rows fold into this one and stop being separate asks:** "Second + third Supabase projects" (the production project is created here) and "Rename the Android package `com.swingsage.spike` → `com.swingsage.app`" (the rename and the permanent signing identity are set together, here, because both are irreversible from the first store upload). The "Decide the analyzer worker host" row is ANSWERED — Modal. **R2 IS in, corrected 2026-08-22** — an earlier line here said it was not needed because Supabase Storage carried "the storage half of the auth boundary". That was factually wrong: `supabaseStore.ts` holds `SUPABASE_SECRET_KEY`, which is NOT subject to `storage.objects` policies, and its own header records that authorization rests on `requireViewAccess` in the route instead. No storage policy exists. So the live model is private bucket + service credential + signed URL, which is exactly what R2 does — nothing is given up, R2 charges zero egress against Supabase's $0.09/GB past 250GB, and it needs no new vendor (same Cloudflare account as the domain). Supabase Pro is still right, but for **no-pause and backups**, not storage. **Three more signups added 2026-08-22 (D64), all free to start:** **Anthropic Console** — an API key for the coach's words (narrative, chat, launch-monitor screenshot parsing). Paste it as `ANTHROPIC_API_KEY`; it is used BYOK through Vercel AI Gateway, so you pay Anthropic list price with no markup and there is no second bill. ~~Sentry and PostHog~~ — **both DEFERRED by Taylor 2026-08-22 and removed from this ask**; they re-open in `observability-and-slos`, where Sentry is launch-blocking (crash-free sessions has no other instrument). **OpenRouter is explicitly NOT needed** and neither is a secrets vendor (Infisical is struck — secrets live in Vercel/EAS/Modal). **Still NOT needed:** a paid EAS plan (build-queue convenience, not infrastructure). **One thing to know before the first range trip, and it is not solved by any of the above:** `media-pipeline` is not built, so there is no on-device trim or compression — each swing is a ~300MB upload, ≈95-100s on cellular. The production answer is the analysis-proxy-first lever (upload a 3MB analysis copy immediately, the full-quality original in the background — a ~30× transfer win and the largest single item in the latency budget). Claude can land that in `media-pipeline`; until it does, shoot on wifi or a hotspot. |
| OPEN | Walk the Home spotlight carousel and iterate to sign-off (spotlights track step 04 — YOUR gate) | Design judgement — same gate shape as session-mode's D61 | Built 2026-08-24, steps 01–03: Home's first sheet slot is now a swipeable carousel of dismissable cards (center-snapped, neighbours peeking, loops forever) — multiview (placeholder art, → Record), the old deep/stance intro cards (migrated in; a previous X on either stays honored), the Pro card (free tier only, → Upgrade), 240fps capture, plus milestone/anniversary cards that appear at 50 swings / 1 year. Dismissal is per-account on the server now — dismiss on the phone, gone everywhere, forever (DEBUG → Spotlights → "Reset dismissals" brings them back; the SystemGallery style guide has a deck-size harness). Walk it: swipe both directions through the loop, dismiss down to empty, force-stop + relaunch to see nothing come back, both themes; say what reads wrong, iterate, then "signed off". |
| OPEN | Real multiview card art — a photo of an actual two-phone setup + an app screenshot of multiview | Physical asset only you can shoot | The flagship spotlight card ships with placeholder art (the dual-view mark on a dark bed). The composition reserves an art region, so the swap is dropping the image in — no redesign. Shoot a real two-phone setup when convenient; multiview itself is still future work, so this can trail the sign-off row without blocking it (placeholder is a named shortfall, not a silent one). |
| DONE 2026-08-26 | Verdict on the instructor-platform plan (`.claude/architecture/instructor-platform-2026-08-24.md`) — **APPROVED** ("create the track and start"); the `instructor-mode` track is created and running. The §7c open calls were not answered with the approval; student seats got its own row below. | Strategic judgement — it supersedes two recorded decisions and sets the billing shape | The plan: "instructor" everywhere **including internal identifiers** (lifts the 2026-08-19 carve-out — role value, `coach_links`, `/api/v1/coach/*` rename while data is dev-only); the entitlement model goes **two-dimensional** (personal free/pro × instructor membership free/gold/platinum, Gold+ includes Pro); **one live store subscription per account** so a membership upgrade from personal Pro is a store-native prorated crossgrade (one iOS subscription group, Play ReplacementMode) — Taylor's cancel-prorate-upgrade sentence, implemented by the stores; mode toggle in the AppHeader (device-local, role-gated), charcoal `INSTRUCTOR` theme binding; and a **fully mocked instructor UI** (dashboard, roster, student detail, inbox + BCC broadcast, directory listing editor, membership page) built UI-first behind swap seams, membership states forceable in the debug sheet. Say "approved" (or what to change) and Claude creates the `instructor-mode` track and starts step 01. Gold/Platinum pricing + feature split stay TBD until `billing-iap`. **Gap-reviewed 2026-08-24 (doc §7)** — spec audit + competitive study folded in; the mocked UI now covers eight instructor screens + the golfer-side halves, and §7c holds the open calls that are yours: (1) **student seats / sponsored analyses on Gold/Platinum** — the strongest open idea, decide before billing-iap prices tiers; (2) in-person capture (instructor records a student on their own phone) as a future track; (3) one-vs-many instructors per golfer; (4) directory ratings. |
| OPEN | Walk instructor mode end to end on the S25+ and iterate to sign-off (instructor-mode track step 05 — YOUR gate) | Design judgement — the D61 gate pattern; wiring may not start before it | Steps 01–04 built it all, mocked, nothing plumbed. The walk: switch to the **Dave Kim persona** (the instructor — eligibility now flows from the signed-in account; the force toggle is gone) or run Profile → "Become an instructor" (a REAL role claim now), then the header dropdown beside the menu glyph — flip to Instructor: the whole app turns charcoal (blue accents stay), the bar becomes Home/Students/**Broadcast**/Inbox/Profile. Walk: the triage Home (review requests, a regression, a quiet student — tap through), Students (search, group chips, pending + invite QR sheet), a student detail (plan, progress bars, the three review doors on a swing, drills with camera-verified vs self-reported, focus slots — force "slots full" in DEBUG → Instructor mock), the Inbox + a thread (typed cards; force frozen/blocked), the Broadcast composer (audience picker, send toast), Drill library, the Listing editor (force all five lifecycle states), Membership (the refusal demo row). Then the GOLFER half via the persona/instructor flag: the chat shows the same thread from the student side — the broadcast reads personal — with the ask-for-review action; the profile drawer branches per mode. Both membership × personal states are chips in DEBUG → Subscription state. Say what reads wrong, iterate, then "signed off". **Also his call during the walk:** where ask-for-review should live on the swing page itself (currently chat + connected card only — a named deferral). **NOTE:** the phone would not answer wireless adb all session (sweep found nothing on the LAN) — when it's back, say "phone's on" and Claude installs. |
| OPEN | Decide: student seats / sponsored analyses on Gold/Platinum instructor memberships | Product + pricing judgement — it changes the SKU design, so it must land BEFORE `billing-iap` prices tiers | The strongest open idea from the instructor-platform gap review (plan doc §7c): a free student produces no new analyses, so a free-tier roster is dead content; every competitor's coach plan recruits students by bundling access. Options: N student Pro seats included per membership, a pooled analysis allowance the instructor grants, or neither. Also still open from §7c, lower stakes: in-person capture (instructor records a student on their own phone — proposed future track), one-vs-many instructors per golfer, directory ratings. Not blocking the instructor-mode track — the mocked UI renders regardless. |
| OPEN | Record a fresh swing and confirm 240fps end to end — pill, slow-mo ladder, report | Physical device interaction | Built 2026-08-23 and installed on the S25+. What to look at: **(a)** capture screen idle — the FPS pill top right under the heading says `240 FPS` and taps open a dropdown of the lens's real rates; **(b)** while recording — a subtle pill top right shows the rate the take actually configured; **(c)** after analysis, the report's speed rail is now `1x · 1/2 · 1/4 · 1/8`, and at `1/4` and below every sensor frame reaches the screen (true slow-mo). Old swings still say 60 — their artifacts were analysed under the old CFR-60 rule and stay as-is until re-analysed; only a NEW recording (or re-upload) exercises the new pipeline. |
| OPEN | Device pass of the finished session loop on the S25+ — two paths only a real phone makes | Physical device interaction; neither is reproducible on the emulator | `session-mode` step 07 closed on its automated oracles with this as the named shortfall. **(1) Permission revoked mid-session.** Open session mode, background the app, Android Settings → Apps → SwingSage → Permissions → deny Camera, come back. The screen should flip to the "needs the camera and mic" panel rather than leaving a dead preview under a Record button that still looks ready; grant it again from Settings and coming back should restore the picture with **no second permission prompt**. This was a real hole — the grant had only ever been read once at mount, and the Settings door this screen offers is a round trip out of the app, so the answer always arrives while it is backgrounded. **(2) A save on a nearly-full phone.** Fill the device, record, Save. The trim will fail; what must happen is that the swing is still saved (untrimmed — never lose the only copy) AND a toast says "Saved the whole recording / your phone may be out of space". Before today that path was silent, so a 30-second clip where you expected five read as the strike mark not having taken. Needs the current dev build: `pnpm --filter mobile phone:native`. |
| OPEN | Import a REAL Samsung slow-mo gallery clip on the S25+ and watch it analyze at true speed | Physical device interaction — picking a clip from your own gallery is you driving the phone | The video-analysis-redesign step 02 E2E: the source manifest now carries the capture rate that the phone-side trim used to destroy, so a trimmed slow-mo import must analyze at its REAL length (a ~5s swing), not as forty seconds of 30fps video. Walk: film (or reuse) a 240fps slow-mo in the stock camera → SwingSage → Upload → pick it → confirm the auto-cut → after analysis the report should play at real speed with the full swing present, and the job log (web app → the swing's job) should read `source manifest: present (capture 240 …)`. Needs the native dev build Claude installs (`phone:native` — new trim/probe surface). If the analysis instead fails with "too much video to analyze", the manifest did not survive — report exactly that sentence. |
| OPEN | **Verify the ten fixtures' event labels** (video-analysis-redesign step 04) | Ground truth cannot be self-certified — Claude labeled first-pass, the human check is the point | Claude read frame strips of every fixture's `normalized.mp4` and wrote `fixtures/labels/<stem>.events.json` (address/top/impact/finish, frames + ms + per-event confidence, definitions frozen in `services/analyzer/groundtruth/ANNOTATION-MANUAL.md`). To verify one clip: `cd services/analyzer && .venv\Scripts\python.exe scripts\labelstrip.py <stem> <frame> --span 6 --step 1` renders the frames around any label; edit the JSON if a frame is wrong (the file re-validates on next use). The one that matters most: **7wood-1 impact** — the label contradicts the stored artifact by ~40 frames and is what the golden gate keys on. Set `"verified_by": "taylor"` in each file you've checked. |
| OPEN | Club-position labels for the golden set — impact-window ±12 frames per fixture (5-point) | Point-precise labeling needs a human with a pointing device (CVAT or equivalent) | The club evaluator (`groundtruth/evaluate_club.py`) is built and tested but has no positional truth to run on — this is the metric that finally ranks the 35 stored club variants and un-parks two thresholds in `club.py`. Schema + CVAT task conventions: `groundtruth/import_cvat.py` docstring; point definitions (grip, shaft_mid, hosel, head_a, head_b, per-club-type) frozen in `ANNOTATION-MANUAL.md` §1 — read it BEFORE the first label. Start small: impact ±12 frames on one fixture, run `python -m groundtruth.evaluate_club out/<stem> --variants`, see the ranking table work, then grow. Alternative zero-setup path: place head markers in the web player's marker editor and Claude exports them via `import_head_markers.py` (head-center-only labels — enough for PCK/median error, not shaft angle). |
| OPEN | New footage for the ground-truth holdout tier: a face-on set, a left-handed golfer, outdoor, other clubs | Only you can film it (and recruit the left-handed golfer) | The golden set is 10 clips, ALL down-the-line and right-handed — every accuracy number is single-view single-handedness until this exists, and the holdout tier (`groundtruth/goldenset.json`) is empty because it must be golfer- AND recording-disjoint. What unblocks the most, in order: (1) a face-on set of your own swings (any club, the bay is fine), (2) any left-handed golfer, even a few swings, (3) an outdoor session, (4) driver + wedge takes. Phone originals, no trimming needed — drop them anywhere and tell Claude, who runs the pipeline and files them into tiers. |
