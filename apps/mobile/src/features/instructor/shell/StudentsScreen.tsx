import { PlaceholderScreen } from "./PlaceholderScreen";

/** Step-04 seam: the roster (architecture §4a.2) lands here. */
export function StudentsScreen() {
  return (
    <PlaceholderScreen
      title="Students"
      note="Your roster — students, groups and requests — is built in the next step."
    />
  );
}
