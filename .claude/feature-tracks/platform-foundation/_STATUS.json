{
  "schemaVersion": 1,
  "featureName": "platform-foundation",
  "currentStep": "10",
  "phase": "Platform Foundation",
  "lastUpdated": "2026-08-23T20:05:00Z",
  "steps": {
    "10": {
      "status": "in-progress",
      "startedAt": "2026-08-23T17:55:00Z",
      "note": "Order override at Taylor's direction 2026-08-23 ('go' on the off-LAN plan): 10 ran before 08. DAY-ONE SUBSTANCE SHIPPED, step stays in-progress on the named remainder. DONE: swingsage-prod fully migrated (19 migrations via MCP, drizzle journal stamped, swingsage_app role live THROUGH THE POOLER \u2014 aws-0-us-east-1, SCRAM verifier required, md5 refused; proven: connect + set local role authenticated + RLS answers; 13-table parity, 0 advisors). r2Store.ts driver live and proven on the real bucket (206 ranges via signed GET, presigned PUT, move/remove) \u2014 step 09's appended amendment closed. Vercel production DEPLOYED and VERIFIED at www.swingsage.io: /api/v1/client 200 contract, swings 401/401, forced-upgrade 426 against the REAL deployed API, /signin 200. 20 env vars mapped (auth interim golf-swing / data swingsage-prod split \u2014 decisions entry + cutover HANDOFF row; JOBS_CLUB_VARIANTS=false in prod \u2014 decisions entry). Deploys are node scripts/deploy-web.mjs (local build + --prebuilt): Vercel's Linux builders split this app past Hobby's 12-function cap and the Windows local build needed five defect classes repaired (backslashed symlinks; filesets missing .next chunks, next runtime, dep packages; 4k+ over-traced local files incl .env \u2014 the D43 guard caught that one). Release APK built (universal, Vercel URL baked, Metro-free) \u2014 install blocked ONLY on the phone being reachable (HANDOFF row). REMAINING for this step: Linux CI build+oracle gating (retires deploy-web.mjs), preview env (waits Supabase Pro), mobile store distribution (BLOCKED Taylor rows: Apple, package rename), deploy/rollback docs in ARCHITECTURE.md, and the first real swing through the hosted loop (Vercel->QStash->Modal->Vercel \u2014 needs the phone)."
    },
    "01": {
      "status": "complete",
      "completedAt": "2026-08-08T23:30:00Z"
    },
    "02": {
      "status": "complete",
      "completedAt": "2026-08-11T19:00:00Z",
      "note": "All probes measured on a Galaxy S25+ over wireless debugging (D34-D40). Overlay 99.2% frame-locked with ~49ms draw budget; seeking 100% frame-exact once the target became frame/fps (media3 resolves seeks FORWARD - the web player's midpoint rule is wrong on Android); network adds ZERO seek error; 1080p at 231fps via Camera2 constrained high-speed. D5's Expo/RN choice is confirmed on Android. Two modules must survive the spike: frame-clock and high-speed-camera. Open and named: scrubbing unmeasured (reassigned to measure_overlay.py), 231 vs 240 is 3.6% short, iOS entirely untested (no Mac/device, non-gating per D31)."
    },
    "03": {
      "status": "complete",
      "completedAt": "2026-08-11T16:45:00Z",
      "note": "D42 closes the gap D26 opened. The app connects as swingsage_app (NOINHERIT, no superuser, no BYPASSRLS, not a member of service_role) and every query goes through withUser() - a transaction that sets request.jwt.claims and `set local role authenticated`, both reverting on commit. The ambient db export is DELETED (src/db/client.ts is gone); the data modules take the transaction as their first argument. withUser asserts four properties against the live connection and refuses to serve otherwise - verified by pointing APP_DATABASE_URL at the owner, which fails 11 of 12 boundary tests naming the role. app.ensure_profile() (SECURITY DEFINER, identity from auth.uid() internally, in a schema PostgREST does not serve) removes the last elevated write from a request path; Supabase's advisor caught it while it was in public and anon could call it over REST. Also found: the hosted project was three migrations behind (0005/0006/0007 local-only), now applied - both databases agree at ten tables, RLS forced, 2 policies each, zero advisor findings. Carried forward deliberately: one Supabase project rather than three (D10, money, step 10) and the analyzer service role unscoped to specific tables (defined by the analyzer-service track)."
    },
    "04": {
      "status": "blocked",
      "note": "D43 (2026-08-11): Google native sign-in is live on Android and the server accepts a native session as a bearer token; web tsc/lint clean, 157 vitest, 84 jest, 100 schema vitest, Android assembleDebug SUCCESSFUL with RNGoogleSigninPackage linked, and a real Supabase session proven end-to-end against the running server (valid token 200, garbage token 401 rather than the dev fallback, new account sees 0 swings). Two traps found by inspection: the DEV_USER_EMAIL fallback would have answered the phone's requests regardless of its token, and the development identity squatted Taylor's real address against a UNIQUE users.email so the first real Google sign-in would have raised a unique violation. Still open: on-device verification (phone not reachable - wireless debugging off; procedure in RUNBOOK section 6), phone OTP (needs the local Supabase stack), Apple ($99 + hardware), real SMS (A2P 10DLC), account deletion, identity linking, and the deletion of email OTP / seeded admin / DEV_USER_EMAIL which D31 gates on phone also being live. | D44 (2026-08-12): on-device verification DONE - Google sign-in works on the S25+, session survives restart, new account sees 0 swings. The screen behind sign-in was the step 02 probe harness; it is deleted (src/spike/, six probe scripts, the synthetic clip, ServerCheck, expo-asset, the :8790 fixture server) and replaced by src/screens/HomeScreen.tsx, which distinguishes 'no swings' from 'could not ask'. modules/frame-clock and modules/high-speed-camera kept and now consumer-less by design. Cost named: measure_overlay.py is gone, so scrubbing stays unmeasured and moves to mobile-player. Verified: web tsc/lint, 157 vitest, mobile tsc, 34 jest, Android assembleDebug SUCCESSFUL + installed, served bundle has zero spike strings. Step stays open on: package still com.swingsage.spike (needs one Google Cloud Console visit to add an OAuth client for the new name before renaming), phone OTP, Apple, account deletion, identity linking, and deleting DEV_USER_EMAIL. | D45 (2026-08-12): the account LIFECYCLE half closes. Migration 0009 adds app.delete_own_account() - SECURITY DEFINER, no argument, identity from auth.uid(), so deleting another account is not expressible - and makes users.email NOT NULL with a matchable SS_EMAIL_REQUIRED from ensure_profile, landing D31's email invariant BEFORE phone, the provider that can violate it. DELETE /api/v1/account orders the cascade media -> rows -> identity so every failure is retryable; lib/account/identity.ts is the single fenced auth-admin call site and service-role.test.ts fails on a second one. Mobile gets a delete-account screen that states the six consequences before the control and confirms by typed word. Verified: web tsc/lint, 167 vitest (+10), mobile tsc, 39 jest (+5), 100 schema vitest, and `pnpm --filter web verify:account` - 7/7 against the RUNNING system, including two concurrent sessions on one account (200/200), a local sign-out leaving the other alive, a global sign-out demonstrably killing it, DELETE returning 200, getUserById finding nothing, and a still-unexpired token answering 401. Found while running it: deleting a hosted auth identity does NOT remove the local public.users mirror, so the next sign-in under that address 500s on the UNIQUE email - D43's collision from the other direction, recorded in ENVIRONMENT.md. Step stays open on: phone OTP (needs the local Supabase stack; no supabase/ dir yet), identity linking (needs a second provider to link to), Apple ($99 + hardware), real SMS (A2P 10DLC), the package rename (Console visit), and deleting DEV_USER_EMAIL. On-device look at the new screen is outstanding - the bundle Metro serves contains it, but the phone was in its owner's hands. || BLOCKED 2026-08-18: the substance shipped (Google native sign-in live, real Supabase sessions end-to-end, server accepts a native session as bearer). Everything still open needs Taylor and is already a BLOCKED row in docs/HANDOFF.md - Apple sign-in ($99 enrolment + Mac/hardware), real SMS OTP (A2P 10DLC registration), the com.swingsage.spike -> com.swingsage.app rename (Console visit). Reclassified in-progress -> blocked so the track can advance; step 05 proceeds because its stated dependency is that real identities exist to attach roles to, which is satisfied."
    },
    "05": {
      "status": "complete",
      "startedAt": "2026-08-18T19:28:05Z",
      "note": "BACKEND HALF DONE 2026-08-18 (commit 82e79ff), mobile onboarding UI outstanding. Landed: migration 0012 - user_roles (no INSERT policy; app.claim_role whitelists golfer|coach, admin unclaimable, identity from auth.uid()), golfer_profiles (all of 5.2/5.4/5.5, private by construction - the public/private split is expressed as WHICH TABLE a column is in, not a per-column flag), golfer_goals (curated eight, ranked, capped at 3 by an AFTER ROW trigger so a four-row single insert cannot slip past a per-row count). handedness+height_cm moved off users (no readers) onto the profile; swings.handedness stays NOT NULL. API: GET/PATCH /api/v1/profile, GET/POST /api/v1/roles, and the first role-gated route GET /api/v1/coach/roster behind requireRole() -> 403 role_required. Section 43 answers recorded in docs/decisions/auth-identity.md (min age 13 self-attested; age is a RANGE never a birthdate; handedness the only required answer, nullable in schema because required is a property of the FLOW - a NOT NULL would make a half-finished profile unresumable). Gates: web tsc+lint clean, vitest 227 (+35), mobile tsc clean, analyzer 189/2s/1x, migration applied. REMAINING for Definition of Done: the mobile onboarding flow (role -> handedness -> style/goals/skill, all skippable but handedness, resumable off onboarding_completed_at), wiring ProfileScreen/GoalsScreen to the new routes, and having the enqueue path default handedness from the profile. || MOBILE HALF DONE 2026-08-20: profile wire types added to the shared contract (packages/schema api.schema.json definitions handedness/golferGoal/profilePublic/golferProfilePrivate/profileResponse/profilePatchRequest/rolesResponse; generated, shape-lock relocked additively - 327 insertions, 0 deletions). Mobile: useProfile store (module cache + optimistic saveProfile with revert-on-failure, cleared at the auth boundary), profileFields registry (every 5.2/5.4/5.5 field with golfer-language labels, sections tiered per 5.5, displayValue + completeness derived from the registry so a new field is ONE entry), FieldEditorSheet (choice/number/multi/mobility editors in one sheet; choices save-and-close on the tap, second tap clears), GoalPicker (rank = tap order, cap 3 enforced in toggleGoal), GoalsScreen rebuilt live, MyProfileScreen (value tiles + completeness meter, wired from the profile drawer), OnboardingScreen (role -> handedness -> style -> goals -> skill; handedness the only unskippable step; every tap saves so the profile row is the draft; completeOnboarding stamps on finish), OnboardingLauncher (auto-opens once per app launch while onboardingCompletedAt is null; debug-menu Run onboarding action). BONUS scope (Taylor, same session): left-handed mirroring - PoseOutline.mirrored + posePlacement(mirrored) reflect the capture art, AlignmentGhost/ViewToggle/DualSyncPip/AnalysisErrorSheet follow profile handedness, and SessionScreen swaps the control/sync rails so zoom + camera flip sit on the RIGHT for a lefty. Gates: mobile tsc clean + 411 jest (+4 suites), web tsc/lint clean + 235 vitest, schema 100 vitest + tsc. NAMED SHORTFALL: the enqueue-path handedness default has no seam yet - no client enqueue path exists (import door is session-mode step 06, capture upload is swing-ingest); recorded there rather than held open here. On-device walk of onboarding + My profile is Taylor's (HANDOFF row).",
      "completedAt": "2026-08-20T19:58:00Z"
    },
    "06": {
      "status": "complete",
      "completedAt": "2026-08-11T03:35:00Z",
      "note": "Shipped in two halves by D28: the additive equipment/session/organization model (migration 0005), then the \u00a77.1 multi-view restructure and uuid swing identity (migration 0006, D30). All ten analysed fixtures survived with their scores; verified by src/db/multiView.test.ts and the Playwright end-to-end path."
    },
    "07": {
      "status": "complete",
      "completedAt": "2026-08-11T14:45:00Z",
      "note": "D41. packages/schema now holds JSON Schema for analysis.json, coach_report.json, silhouette.json and every API body; TypeScript is generated from it and both clients import it - apps/web's ~300-line hand-written Analysis/Scorecard block is deleted. The analyzer validates against the SAME schema files before writing (swingsage/contract.py; burnin, rescore, resegment). schemas/shape-lock.json makes the additive-only rule a test: 526 locked nodes, and a removal/retype/new-required/dropped-enum fails the suite. Every route moved to /api/v1/ with a route test that fails on anything unversioned, plus GET /api/v1/client and a 426 UpgradeRequired path enforced in proxy.ts and rendered as a terminal screen on mobile. First .github/workflows/ in the repo. Tightening the schema surfaced ~96 places in the player that assumed a block an older artifact may not carry."
    },
    "08": {
      "status": "not-started"
    },
    "09": {
      "status": "complete",
      "completedAt": "2026-08-11T10:05:00Z",
      "note": "Media addressed by identity rather than folder name (D33): a lib/media seam with a credential-free local driver and a Supabase Storage driver, artifacts published from the analyzer's working directory, and both buckets live in golf-swing. Range requests verified over the network path (206, bytes 1000-2999/5496355). Deviations: media_key was NOT rewritten into a prefix (keys are derived, not stored) and the analyzer was not modified (publishing is a separate step) - zero diff under services/analyzer. Open: buckets exist in one environment, not three (D10); storage-level RLS deferred with D24."
    }
  },
  "blockers": [
    {
      "step": "04",
      "kind": "USER-ACTION-NEEDED",
      "summary": "Apple Developer enrolment, A2P 10DLC, Android package rename - all OPEN/BLOCKED rows in docs/HANDOFF.md",
      "recordedAt": "2026-08-18T19:28:05Z"
    },
    {
      "step": "10",
      "kind": "USER-ACTION-NEEDED",
      "summary": "Phone unreachable (wireless debugging off) \u2014 the release APK install + first hosted swing are the last mile; HANDOFF row added 2026-08-23",
      "recordedAt": "2026-08-23T20:05:00Z"
    }
  ],
  "skipped": []
}
