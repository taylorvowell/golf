/**
 * The product's name as it renders in UI. Settled 2026-08-14: the real logo's wordmark reads
 * *Swingsage*, so every "Ideal Swing" string in the reference mockups renders as SwingSage.
 * One constant so a future rename is one edit; the SVG lockup lives at
 * `assets/brand/swingsage-logo.svg` (step 02 builds `BrandLogo`/`BrandMark` from it).
 */
export const BRAND = "SwingSage";
