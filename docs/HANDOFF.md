# Handoff Register — what needs a human, and what already got one

Every task that needs **Taylor** rather than Claude lives here, and **only** here.

`scripts/env-probe.mjs` reads this file at session start and prints every `OPEN` row into the
session, so an outstanding human task arrives in context the same way the phone's LAN address
does. That is the point of the file: a hand-off that lives in prose gets re-asked, and a task
that was already done gets asked for a second time. Both have happened.

## Rules

1. **Never ask Taylor for anything that is not an `OPEN` row here.** Before writing any hand-off
   sentence, check this table. If the ask is not a row, add it as a row first — the act of adding
   it is what forces the check for whether it is already `DONE`.
2. **Never invent a row for something Claude can do.** Money, hardware, an interactive login, a
   vendor dashboard, deleting user data, a production deploy. Everything else is Claude's job —
   "run this command" is not a hand-off.
3. **When Taylor says he did something, mark it `DONE` in the same turn**, with the date and what
   changed. Do not leave it in chat.
4. **A `DONE` row is never deleted.** It is the answer to "did we already do this?" — the exact
   question that gets asked wrong. Rows are only removed when the whole area is retired.

## Status values

`OPEN` — needed now, printed at session start · `BLOCKED` — needed, but waiting on something else
first (state what) · `DONE <date>` — completed, keep for the record · `DROPPED` — no longer needed

<!-- PROBE-READS-BELOW — env-probe.mjs parses the table rows under this marker -->

| Status | What | Why it needs a human | Notes |
|---|---|---|---|
| DONE 2026-08-11 | Google OAuth **web** client created | Google Cloud Console — interactive login | `665583572860-v7lsnd65s2pmr9g4qu29jurb7gqoltk4.apps.googleusercontent.com`. This is the id the app passes to `GoogleSignin.configure`. |
| DONE 2026-08-11 | Google OAuth **Android** client created | Google Cloud Console — interactive login | `665583572860-tlfq1jrit3g4hrmoatlcub81r6t13gn8.apps.googleusercontent.com`, bound to `com.swingsage.spike` + SHA-1 `5E:8F:…:F6:25`. Must exist; never appears in the bundle. |
| DONE 2026-08-11 | Google provider enabled in Supabase, with Authorized Client IDs | Supabase dashboard — interactive | Web client id + secret in the provider; both client ids in the authorized list. |
| DONE 2026-08-11 | `apps/mobile/.env` populated | Holds real (public) values; gitignored | 5 `EXPO_PUBLIC_*` keys set. `.env.example` carries the same public ones. |
| DONE 2026-08-11 | Android wireless debugging paired | Physical device interaction | Pairing survives reboots; the **port changes** and must be read off the phone each time. |
| OPEN | Fix the `ANDROID_SDK_ROOT` user environment variable | Windows user env vars — GUI | Its value contains its own name, so every Android build needs the inline override. Settings → System → About → Advanced system settings → Environment Variables → User → `ANDROID_SDK_ROOT` = `C:\Users\taylo\AppData\Local\Android\Sdk` |
| BLOCKED | Rename the Android package `com.swingsage.spike` → `com.swingsage.app` | Needs a new Android OAuth client first (Console, interactive) | Permanent from the first store upload; visible in the Play Store URL forever. **Blocked on:** one additive Android client for the new package on the same SHA-1. Free. Claude does the rename once it exists. |
| BLOCKED | Apple Developer Program enrolment ($99/yr) | Money | Needed for Sign in with Apple and any iOS build. Nothing iOS has ever been compiled — there is no Mac. |
| BLOCKED | A2P 10DLC registration | Money + a business identity | Gates real SMS delivery. **Phone OTP is HELD entirely as of 2026-08-12 (D46)** — no SMS provider is set up and the build moved to core functionality. The free development path (a local Supabase stack's test OTP) needs nothing from Taylor and can resume whenever this is picked back up. |
| BLOCKED | Second + third Supabase projects (preview, production) | Money — production tier is $25/mo | Preview is free. Three environments is a recorded decision that is currently unmet. |
| DONE 2026-08-12 | Seek exactness confirmed on the S25+ | Physical device interaction | Taylor: *"seek looks good."* The figure directly observed was **30 seeks · 100.0% exact · p95 0 · max 0**; the larger run was confirmed but not read back, so `CURRENT-STATE.md` §11b claims only n=30 and names the shortfall. `mobile-player` step 01 closed on this. Re-run any time: RUNBOOK §11. |
| DONE 2026-08-13 | Add a Google account to the desktop emulator | Credential | This is the **only** thing standing between Claude and verifying UI on its own — with it, the emulator replaces most "please look at your phone" asks permanently. The `swingsage` AVD is built, tuned and has the app installed and talking to Metro; it stops at Google's *"Checking info…"* because a fresh AVD has no Google account, so everything behind the auth gate (swing log, player, overlays, Analysis, Compare) is unreachable on it. **Start it with `~/AppData/Local/Android/Sdk/emulator/emulator.exe -avd swingsage -gpu host` (or ask Claude to), then in the emulator: Settings → Passwords & accounts → Add account → Google.** It persists in the AVD across reboots and reinstalls. Any Google account works — it does not have to be the one on your phone, as long as its email is in `AUTH_ALLOWED_EMAILS`. Signed in as `taylorvowell@gmail.com`; **verified to survive a full emulator kill and relaunch**, so Claude can restart it unattended. RUNBOOK §13. |
| OPEN | Decide the analyzer worker host | Strategic + spend | **The measurement is now done (D53) — this is no longer a guess.** Pose is **2.32x** faster on CUDA than CPU (70.4 -> 30.4 ms/frame), measured on a **GTX 1080, a 2016 card** — so a modern cloud GPU is the floor of that, not the ceiling. Output agrees to **0.94 px**, so a GPU host does not change the analysis. What it means: **Railway having no GPU is a real cost, not a theoretical one** — roughly double the analysis latency per swing, which lands directly on the §38 SLO. The choice is yours because it is spend: a GPU host (Modal / RunPod / Fly GPU / a GPU VM) against Railway-on-CPU with slower analysis. Tell Claude which and the track continues; there is nothing else Claude needs to unblock it. |
| DECLINED 2026-08-13 | Read the overlay's frame-lock off the S25+ | Physical device interaction | Taylor: *"I don't want to measure. skip this."* Declined while **available**, not while blocked — the phone was connected (`10.0.1.123:39593`) and `adb shell input` was landing again. `mobile-player` step 02 closed carrying it as a named shortfall, and **the Skia question (D23/D36) stays open on it — see D51**. Do **not** re-ask; if the trace ever looks like it stutters, this is the number that settles it: RUNBOOK §12b, about a minute. |
| OPEN | Design verdict on the swing screen + the two-decoder reading | Design judgement + a phone-only measurement | **Both features are now verified to WORK** — Claude drove the desktop emulator and confirmed the Analysis panel renders and its taps seek to the right frame, and that side-by-side comparison holds two swings at the same position while scrubbing. What is left is genuinely yours: **(a)** whether the full-bleed screen, the scrubber, the overlay tiles, the Analysis panel and the side-by-side layout *look right* — taste, not correctness; **(b)** the one number the emulator cannot produce, because it is software-rendered: open a swing on the **S25+**, turn on Compare, and read **Overlay drift** in METRICS with two videos running. If it holds, `mobile-player` step 04's open question is closed. |
| OPEN | Reinstall the dev build on the S25+ (adds `react-native-svg`, light/dark theme, `expo-font` + `expo-linear-gradient` + the Ideal Swing palette) | Physical device interaction | Three changes are stale on your phone's dev client: the after-swing gauges added the app's first SVG native module (crashes the after-swing screen until reinstalled), the theme system flipped `userInterfaceStyle` `dark` → `automatic` — until the rebuild, the OS always reports dark, so the new light-default only appears via Settings → Appearance → Light, never from "Match my phone" — and design-system step 01 (2026-08-14) added `expo-font`/`expo-linear-gradient` (two more native modules) plus the new navy/cobalt/aqua palette and bundled fonts. Because native config and modules changed, the rebuild needs a clean prebuild: `cd apps/mobile && npx expo prebuild -p android --clean && npx expo run:android --device` — or just ask Claude with the phone connected. The desktop emulator was rebuilt with all of this 2026-08-14. |
| DONE 2026-08-14 | Brand wordmark verdict: "Ideal Swing" (the mockups) vs "SwingSage" (the app today) | Product naming — judgement call | **Settled by Taylor providing the real logo the same day** — its wordmark reads *Swingsage*, so the brand is **SwingSage** and every "Ideal Swing" string in the mockups is replaced. Master lockup (mark + wordmark, white-on-dark, aqua `#42CBCE`/`#3FFFF5` swoosh) saved at `apps/mobile/assets/brand/swingsage-logo.svg`; the design system renders it via a `BrandLogo`/`BrandMark` component (design-system step 02). |
| OPEN | Device pass of the rebuilt Swing Report (video-open) on the S25+ | Physical device interaction + a phone-only measurement | Design-system step 07 made the swing screen the mockup's sheet-over-video: the report sheet rides over the **live player**, dragging it down enters **video-open** (player controls slide in, pill nav out, sheet drops away), with a phase-labelled scrub. Verified working end-to-end on the emulator, which cannot measure: on the S25+, open a swing from the log, scroll into video-open, scrub, and confirm frame-lock/seek feel (the real numbers, if wanted, are RUNBOOK §11/§12b on the after-swing player — same transport). Needs the reinstall row above first (same dev client, no new native modules). This is step 07's named device-pass shortfall. |
| DONE 2026-08-17 | Replicate API token in the voice-bank generation script's env | Credential — Taylor's existing Replicate account | Taylor provided it (originally into `apps/mobile/.env`; Claude moved it to **`apps/web/.env`** — server-side, gitignored, probe-reported — since the consumer is the server-side D57 generation script, never the app). env-probe now prints `REPLICATE_API_TOKEN=set`. Supersedes the earlier Google AI Studio key ask: the chosen model runs on Replicate directly (`replicate.com/google/gemini-3.1-flash-tts`). Vendor call + addendum in `.claude/architecture/voice-tts-vendor-2026-08-14.md`. |
