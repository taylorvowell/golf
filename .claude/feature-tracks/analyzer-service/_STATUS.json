{
  "schemaVersion": 1,
  "featureName": "analyzer-service",
  "currentStep": "05",
  "phase": "Platform Foundation",
  "lastUpdated": "2026-08-13T21:12:52Z",
  "steps": {
    "01": {
      "status": "complete",
      "completedAt": "2026-08-13T09:45:00Z",
      "note": "MEASURED: CUDA is 2.32x faster than CPU for pose (70.4 -> 30.4 ms/frame) on a GTX 1080, and agrees to within 0.94px across 6150 keypoints (worst conf delta 8e-6) - sub-pixel but NOT bit-identical. Three false results came first, all the same shape - CUDA silently falling back to CPU while the benchmark reported the non-difference: (1) the installed onnxruntime was the CPU-only build, so device='cpu' was not even the binding constraint; (2) onnxruntime-gpu 1.28 wants CUDA 13 on a CUDA 12.6 machine - it ADVERTISED the provider, failed to create it, and reported 0.98x; (3) pose_device()'s forced-device branch returned before the torch import that registers the CUDA DLLs, so the one caller that forces cuda (the benchmark) was the one that skipped enabling it - 1.00x. Each reads as 'the GPU does not help', which would have chosen the production worker host off CPU-vs-CPU. The durable fix is that posebench.py now asks the REAL session get_providers() and refuses to time a fallback. Analyzer suite green: 123 passed, 2 skipped, 1 xfailed."
    },
    "02": {
      "status": "complete",
      "completedAt": "2026-08-13T17:42:06Z",
      "note": "The full pipeline composition moved out of burnin.py main() into swingsage/pipeline.py: run(AnalysisRequest, on_event) with typed PipelineEvent progress and PipelineResult; burnin.py is now a thin CLI over it. Fidelity proven the strong way: pre- and post-refactor CPU runs of fixtures/pro_2.mp4 produced BYTE-IDENTICAL analysis.json AND coach_report.json at tol 0.0 (scripts/compare_analysis.py), and all 11 stage lines jobs.ts regex-parses still appear (stdout is a protocol until the worker consumes on_event in-process). Suite: 129 passed, 2 skipped, 1 xfailed - 6 new tests pin CLI-flag<->AnalysisRequest default equivalence and OutputLock behavior (now PipelineError, catchable by a worker)."
    },
    "03": {
      "status": "complete",
      "completedAt": "2026-08-13T19:20:00Z",
      "note": "The environment is reproducible and the worker has a skeleton. requirements.txt exact-pins the measured configuration (torch 2.13.0+cu126, onnxruntime-gpu 1.22.0, mediapipe 1.0.0, ...; rtmlib installed --no-deps because its metadata hard-requires CPU onnxruntime - the step-01 silent-fallback vector). services/analyzer/Dockerfile builds from the REPO ROOT context (8.4GB, python:3.13-slim + ffmpeg) so the image ships packages/schema/schemas with SWINGSAGE_SCHEMA_DIR pointing at them - producer-side validation stays ACTIVE in the container. service/worker.py: versioned job-spec JSON -> AnalysisRequest -> pipeline.run(), one JSON object per line; strict validation (unknown fields refuse, club detector never defaulted). Two latent bugs fixed en route: contract.py computed parents[3] at IMPORT time (crashed any monorepo-less install before the documented SWINGSAGE_SCHEMA_DIR override could apply) and test_contract.py had the same walk. Gates: local 141/2s/1x; docker build clean; IN-CONTAINER suite 103/13s/1x exit 0 - the from-scratch reproducibility proof. Club-head best.pt remains a local-only asset with no fetch path (open, later step)."
    },
    "04": {
      "status": "complete",
      "completedAt": "2026-08-13T21:12:52Z",
      "note": "The queue loop is BUILT AND PROVEN locally, host-agnostically: dispatch (lib/jobs/dispatch.ts, behind JOBS_DRIVER=queue - opt-in, spawn stays default) -> QStash local dev server -> worker HTTP server (service/server.py, signature-verified, single-flight) -> source download -> pipeline.run with throttled event forwarding -> artifact PUTs -> verified done callback -> Postgres job state and view flip. E2E harness (pnpm --filter web queue:e2e) PASSED on pro_2: job done, view ready at revision 2, fps 60. The worker holds no DB/storage credential - only URLs plus an HMAC-signed per-job token whose claims carry {jobId, viewId, actorId, targetRevision, exp}; internal routes (/api/internal/jobs/*) verify it and write as the enqueuing user (no elevation on a request path, D26). Failure taxonomy is load-bearing: PipelineError acks 200 (refusals are answers, never retried); infra failures 5xx into QStash's retry schedule. done is verified, not believed (isPublished at targetRevision before the row flips). WORKER_CLUB_DETECTOR must be explicit (path or 'none') - the standing club-detector trap, now enforced at enqueue. Gates: analyzer 165/2s/1x (+24 new); web tsc+lint clean, vitest 179; schema additive (Job.runner) locked, 100 passed; container rebuilt with qstash 3.4.0 pin, in-container 127/13s/1x. Deploy + production QStash credentials + fair queuing/retry/DLQ are later steps; worker host stays the OPEN handoff row."
    },
    "05": {
      "status": "not-started",
      "note": "Declared, file not yet authored (lazy). Natural scope per the track goal: per-user fair queuing (QStash flow-control keys at enqueue), retry/dead-letter policy (failure callbacks + DLQ), remote-orphan detection for queue jobs (heartbeat/timeout - reconcile() is spawn-only today), backpressure, and the capacity/cost model feeding the analysis-latency SLO. The deploy half still waits on the OPEN worker-host handoff row (Taylor, spend)."
    }
  },
  "blockers": [],
  "skipped": []
}
