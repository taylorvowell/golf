import { PlaceholderScreen } from "./PlaceholderScreen";

/** Step-04 seam: the triage dashboard (architecture §4a.1) lands here. */
export function InstructorHomeScreen() {
  return (
    <PlaceholderScreen
      title="Instructor mode"
      note="Your dashboard is on its way — review requests, student swings and roster signals will land here, already analysed."
    />
  );
}
