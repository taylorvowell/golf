# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Binding rules for this app

`.claude/rules/react-native.md` (repo root) is the standing rulebook for all mobile code — hot-path
render discipline, the analysis.json contract on the phone, lifecycle, CNG/native config, and
accessibility. `docs/decisions/mobile-client.md` holds the decisions behind it. Read both before
changing the player, the native modules, or any native config.
